# Build
app/backend/target/
app/frontend/node_modules/
app/frontend/dist/
app/frontend/.vite/

# Runtime
data/sessions/
app/backend/data/

# Marker volatile dell'agente attivo (locale, non committare)
.claude/active-agent

# Environment
.env
*.env.local

# Dati di test — solo per branch feature/test, mai su main/stable
# Il hook check-test-data.sh blocca il push se questi file vengono committati su main/stable
